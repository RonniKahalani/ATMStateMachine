package com.example.atm.bad;

/**
 * Enum to represent user actions
 */
public enum UserAction {
    INSERT_CARD,
    ENTER_PIN,
    SELECT_WITHDRAWAL,
    WITHDRAW_AMOUNT,
    EJECT_CARD,
    SHUTDOWN
}
